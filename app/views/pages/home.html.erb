<div class="banner">
  <div class="container">
    <h1>SpaceBnB brings closer the Outer Space!</h1>
    <p>Travel throught Space just in a clik.</p>
    <a class="btn btn-flat"<%= link_to("All travels", travels_path) %>></a>
  </div>
</div>

<div class="container">
  <h2>Willing to leave ?</h2>
<div class="container-top">
<% Travel.last(9).each do |travel| %>
<div class="card-top">
<% if travel.photo.attached? %>
          <%= cl_image_tag(travel.photo.key, height: 300, width: 400, crop: :fill) %>
           <% else %>
            <img src="https://www.forbes.fr/wp-content/uploads/2019/12/960x0-11-e1576486629882-740x370.jpg" alt="">
  <% end %>
  <div class="card-top-infos">
    <h2><%= link_to("#{travel.destination} for #{travel.organiser.first_name}", travel_path(travel)) %></h2>
    <p><%= "#{travel.organiser.first_name} is proposition #{travel.number_of_travelers} seats in his brand new #{travel.name_of_vehicle} on the #{travel.departure_date}" %></p>
  </div>
</div>
<%end%>
</div>
</div>
